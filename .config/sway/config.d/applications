set $menu albert show
set $term gnome-terminal

# Background Stuff
exec nextcloud
exec albert
exec redshift-gtk
# hidpi workaround for some applications
exec xrdb -load ~/.Xresources
# exec ruby ~/.config/sway/background_changer.sh
exec mako --default-timeout 10000
exec libinput-gestures-setup restart
