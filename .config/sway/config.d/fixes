# Fix for gtk+ applications taking too long: https://github.com/swaywm/sway/wiki#gtk-applications-take-20-seconds-to-start

# exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
# exec hash dbus-update-activation-environment 2>/dev/null && \
     # dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK

for_window [app_id="ulauncher"] border none
# This should only be notifications
for_window [title="TelegramDesktop"]{ \
  floating enable \
}

# For use with sway-hidpi-git
xwayland force scale 2
exec xsettingsd

for_window [title="alacritty floating"] floating true
for_window [title="Volume Control"] floating true
